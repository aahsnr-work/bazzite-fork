# Determinate Nix lives on the persistent /var so it survives image rebase.
# The /nix symlink is recreated on every boot in case the deployment root
# does not carry it.
d /var/nix 0755 root root - -
L+ /nix - - - - /var/nix
